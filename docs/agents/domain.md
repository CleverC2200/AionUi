# Domain Docs

本仓库采用单上下文领域文档。只有新增或修改领域术语、状态权威、跨模块契约或 ADR 时才加载本说明。

执行这类任务时：

- 先在根目录 `CONTEXT.md` 中搜索相关术语，只读取命中的定义和相邻约束。
- 读取与任务直接相关的 `docs/adr/`，不要为无关任务通读全部领域文档。
- 文件不存在时继续执行，不主动创建空文档。
- Ticket、测试和设计使用 `CONTEXT.md` 中定义的术语。
- 若方案与 ADR 冲突，必须明确指出，不能静默覆盖。
