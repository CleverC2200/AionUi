/**
 * GEA will own these remote service endpoints. Keep them disabled until the
 * corresponding GEA contracts, permissions, and destinations are available.
 */
export const GEA_REMOTE_SERVICE_POLICY = {
  autoUpdateEnabled: false,
  feedbackSubmissionEnabled: false,
} as const;
