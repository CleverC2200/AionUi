/**
 * PROTOTYPE ONLY — re-export the production-owned Fixture workbenches so
 * visual experiments cannot become a dependency of the production host.
 */
export * from '@renderer/pages/assistantSurface/fixtures/FixtureWorkbenches';
